var Bonus = function(img, x, y, sx, sy, width, height){
  this.img = img;
  this.x = x;
  this.y = y;
  this.sx = sx;
  this.sy = sy;
  this.width = width;
  this.height = height;
};

module.exports = Bonus
